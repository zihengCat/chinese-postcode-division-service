language: python

sudo: false

addons:
    apt:
        packages:
            - unzip
python:
    - "3.6.6"
    - "2.7.15"

before_script:

script:
    - "PYTHONPATH=`pwd`"
    - "unzip ./db/postcode.zip -d ./db/"
    - "cd ./db"
    - "bash split_postcode_file.sh"
    - "cd ${PYTHONPATH} && python main.py"

after_script:

#branches:
#    only:
#        - "master"
